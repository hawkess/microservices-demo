{
  "name": "ticketing-common",
  "version": "1.0.1",
  "main": "./build/index.js",
  "license": "MIT",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "rm -rf ./build/*",
    "build": "yarn clean && tsc",
    "pub": "git add . && git commit -m \"Updated common libraries\" && yarn version --patch && yarn build"
  },
  "dependencies": {
    "@types/cookie-session": "^2.0.44",
    "@types/express": "^4.17.14",
    "@types/jsonwebtoken": "^8.5.9",
    "cookie-session": "^2.0.0",
    "express": "^4.18.2",
    "express-validator": "^6.14.2",
    "jsonwebtoken": "^8.5.1",
    "typescript": "^4.9.3"
  }
}
